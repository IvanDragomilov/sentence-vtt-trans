import { VTTTranslator } from '@/components/VTTTranslator';

const Index = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-background via-background to-accent/10">
      <div className="container mx-auto py-8">
        <VTTTranslator />
      </div>
    </div>
  );
};

export default Index;
